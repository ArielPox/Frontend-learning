git仓库只能过移出文件的方式有两种
1.git仓库和工作区中同时移除对应的文件 文件状态前面会有绿色的D
git rm -f filename
2.只从git仓库中移除制定的文件 ，但是保留工作区的对应文件
git rm --cached filename

3.以#开头是注释
以/结尾是目录
以/开头是防止递归
以！开头表示取反

4.使用glob模式（简化版的正则表达式）进行文件与文件夹的匹配
4.1*匹配零个或者多个任意字符
4.2[abc]匹配任意一个在[]里面的字符
4.3？只匹配一个字符
4.4[a-z]匹配a-z之间的字符 如0-9就是匹配0到9的数字
4.5**表示匹配任意中间目录